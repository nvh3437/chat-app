
japan-chat-app
composer update 
php artisan key:generate  
php artisan migrate:fresh --seed
php artisan module:seed AvnUser
php artisan module:seed AvnSetting
php artisan module:seed AvnService
php artisan module:seed AvnPost
php artisan module:seed AvnContact
php artisan module:seed AvnChat
php artisan module:seed AvnNewFeed
php artisan module:seed AvnCMS


composer require ably/ably-php

vite production:
    env to production
    npm run build
    manifest to ../public/build/assets/


https://viblo.asia/p/webrtc-va-install-janus-on-ubuntu-1804-gAm5yy885db
https://github.com/meetecho/janus-gateway

apt-get update
apt-get install pkg-config
apt-get install dist
apt-get install make
apt-get install make-guile

# apt-get install zlib lỗi

apt-get install build-essential
apt-get install libssl-dev
apt-get install meson
apt-get install cmake
apt-get install libnanomsg-dev
apt-get install doxygen 
apt-get install graphviz
apt-get install apache2

apt-get install libmicrohttpd-dev
apt-get install libjansson-dev
apt-get install libssl-dev
apt-get install libsofia-sip-ua-dev
apt-get install libglib2.0-dev
apt-get install libopus-dev
apt-get install libogg-dev
apt-get install libcurl4-openssl-dev
apt-get install liblua5.3-dev
apt-get install libconfig-dev
apt-get install pkg-config
apt-get install libtool
apt-get install automake

    cd --
    wget https://github.com/cisco/libsrtp/archive/v2.2.0.tar.gz
    tar xfv v2.2.0.tar.gz
    cd libsrtp-2.2.0
    ./configure --prefix=/usr --enable-openssl
    make shared_library && sudo make install

    cd --
    git clone https://gitlab.freedesktop.org/libnice/libnice
    cd libnice
    meson --prefix=/usr build && ninja -C build && sudo ninja -C build install

    cd --
    git clone https://github.com/sctplab/usrsctp
    cd usrsctp
    ./bootstrap
    ./configure --prefix=/usr --disable-programs --disable-inet --disable-inet6
    make && sudo make install

    cd --
    git clone https://libwebsockets.org/repo/libwebsockets
    cd libwebsockets
    mkdir build
    cd build
    cmake -DLWS_MAX_SMP=1 -DLWS_WITHOUT_EXTENSIONS=0 -DCMAKE_INSTALL_PREFIX:PATH=/usr -DCMAKE_C_FLAGS="-fpic" ..
    make && sudo make install

    cd --
    git clone https://github.com/eclipse/paho.mqtt.c.git
    cd paho.mqtt.c
    make && sudo make install

    cd --
    git clone https://github.com/alanxz/rabbitmq-c
    cd rabbitmq-c
    git submodule init
    git submodule update
    mkdir build && cd build
    cmake -DCMAKE_INSTALL_PREFIX=/usr ..
    make && sudo make install

    cd --
    git clone https://github.com/meetecho/janus-gateway.git
    cd janus-gateway
    sh autogen.sh
    ./configure --prefix=/opt/janus
    make
    make install
    make configs
    ./configure  --disable-rabbitmq
    ./configure --enable-docs

    #run====================================================================================
    cd /opt/janus/bin
    ./janus

    sudo service apache2 start

    #config stun nat====================================================================================
    cd /opt/janus/etc/janus
    nano janus.jcfg
        thêm: nat: {
            stun_server = "stun.stunprotocol.org"
            stun_port = 3478
            nice_debug = false
        }   




chrome://flags
search: Insecure origins treated as secure
enable và nhập ip vào (http://52.221.199.143/) để bỏ qua bảo mật đối với các trang không có tên miền hoặc http//:


???????????????????????
    --libdir=/usr/lib64 